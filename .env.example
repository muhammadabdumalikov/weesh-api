# Database (dev)
ENV=dev
PGHOST=localhost
PGPORT=5432
PGDATABASE=weesh
PGUSER=postgres
PGPASSWORD=

# Or connection string (e.g. production)
# PGSTRING_INTERNAL=postgresql://user:pass@host:5432/dbname

# Wishlist Google Sign-In (same as frontend NEXT_PUBLIC_GOOGLE_CLIENT_ID)
GOOGLE_CLIENT_ID=

# Optional: Telegram auth for wishlist
TELEGRAM_BOT_TOKEN=

# Optional: Weesh-specific Google client (if different from GOOGLE_CLIENT_ID)
# WEESH_GOOGLE_CLIENT_ID=
